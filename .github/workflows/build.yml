
on:
    push:

# can filter jobs using GITHUB_ACTOR=nektos/act

env:
    DOCKER_BUILDKIT: 1

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            -
                name: env
                run: |
                    env
                    git status
            -
                name: build image
                run: |
                    docker build -t test .
            
    